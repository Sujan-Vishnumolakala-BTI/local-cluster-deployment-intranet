apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: etcd-backup-pvc
  namespace: backup
spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: openebs-hostpath

  resources:
    requests:
      storage: 20Gi
